APP_NAME=Bit_micro_serverse

SECRET_KEY=change-me-to-a-long-random-string-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
CORS_ORIGINS=*

# Токен для межсервисных вызовов (internal API)
INTERNAL_API_TOKEN=change-me-internal-token

HEAD_REGISTER_PASSWORD=123456789

PASSWORD_RESET_CODE_TTL_MINUTES=15

# SMTP — отправка кода восстановления пароля на почту.
# Если SMTP_HOST пуст — dev-режим: код печатается в консоль auth-сервиса, письма не уходят.
# Пример для Яндекс.Почты (нужен «пароль приложения», не основной пароль):
#   SMTP_HOST=smtp.yandex.ru / SMTP_PORT=465 / SMTP_USE_SSL=true
# Пример для Gmail: SMTP_HOST=smtp.gmail.com / SMTP_PORT=587 / SMTP_USE_SSL=false (STARTTLS)
# Корпоративный Exchange/почтовый сервер — уточнить хост/порт у системного администратора.
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=Бит.Serves
SMTP_USE_SSL=true

# Порты сервисов (наружу)
AUTH_SERVICE_PORT=8001
PAYROLL_SERVICE_PORT=8002
DASHBOARD_SERVICE_PORT=8003
KP_SERVICE_PORT=8004
FUNNEL_SERVICE_PORT=8007
PIX_SERVICE_PORT=8006

# Адреса сервисов внутри docker-сети
AUTH_SERVICE_URL=http://auth-service:8001
PAYROLL_SERVICE_URL=http://payroll-service:8002

# Экономика расчётов
OFFICE_COST_PER_EMPLOYEE=45000
INSURANCE_RATE_PERCENT=30
VAT_RATE_PERCENT=5
NDFL_RATE_PERCENT=13
FOT_MARGIN_NORMAL_PCT=20
FOT_MARGIN_CRITICAL_PCT=25

# PostgreSQL (у каждого сервиса своя БД)
POSTGRES_USER=bitserves
POSTGRES_PASSWORD=bitserves_password
POSTGRES_PORT=5432
POSTGRES_HOST_AUTH=db-auth
POSTGRES_DB_AUTH=auth_db
POSTGRES_HOST_PAYROLL=db-payroll
POSTGRES_DB_PAYROLL=payroll_db
POSTGRES_HOST_KP=db-kp
POSTGRES_DB_KP=kp_db
